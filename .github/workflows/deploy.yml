name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# 允許 workflow 寫入 Pages，並用 OIDC 換取部署憑證
permissions:
  contents: read
  pages: write
  id-token: write

# 同時只跑一個部署，但不取消進行中的那個
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # 22 是現行 LTS。20 已在退場，且本機是 18（已 EOL），
          # CI 用新一點的版本比較不會突然壞掉。
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build

      # GitHub Pages 沒有 SPA fallback，直接開 /about 或重新整理會拿到 404。
      # 複製一份 index.html 當 404.html，讓 SPA 自己接手路由。
      - name: Add SPA fallback
        run: cp dist/index.html dist/404.html

      # 避免 GitHub 用 Jekyll 處理產物、忽略底線開頭的檔案
      - name: Add .nojekyll
        run: touch dist/.nojekyll

      # enablement 讓 workflow 自己開啟 repo 的 Pages。
      # 少了它，第一次跑會失敗在「Get Pages site failed ... Not Found」，
      # 因為 Pages 還沒被啟用，這個 action 沒有東西可以設定。
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
